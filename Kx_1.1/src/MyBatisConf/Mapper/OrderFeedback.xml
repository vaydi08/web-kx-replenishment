<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "mybatis-3-mapper" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sol.kx.web.dao.order.OrderFeedbackMapper">

	<select id="select" parameterType="orderFeedback" resultType="orderFeedback">
		select * from order_feedback where orderid=#{orderid} order by id desc
	</select>
</mapper>
