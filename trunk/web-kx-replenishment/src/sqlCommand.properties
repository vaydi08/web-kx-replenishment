sql.info.manufactory=select a.id,a.pid,a.name,a.mf_code,a.remark,b.name as pname from info_manufactory a left join info_product b on b.id=a.pid 
sql.info.tessera=select a.id,a.pid,a.name,a.weight,a.clarity,a.color,a.cut,b.name as pname from info_tessera a left join info_product b on b.id=a.pid 